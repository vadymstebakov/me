<main class="main">
    <h1>Vadym Stebakov</h1>
    <p>Hi there! I'm a <i>Front-End Engineer</i>.</p>
    <p><b>Front-End</b> <span class="heart">&#10084;&#65039;</span></p>
</main>

<style>
    @keyframes heartbeat {
        0% {
            transform: scale(0.75);
        }

        20% {
            transform: scale(1);
        }

        40% {
            transform: scale(0.75);
        }

        60% {
            transform: scale(1);
        }

        80% {
            transform: scale(0.75);
        }

        100% {
            transform: scale(0.75);
        }
    }

    .main {
        display: flex;
        flex-direction: column;
        justify-content: center;
        flex: 1 0 auto;
        padding: 1.2rem;
    }

    .main h1 {
        max-width: 550px;
        font-size: clamp(3.75rem, 5vw + 2.75rem, 6.25rem);
        line-height: 1.1em;
        font-weight: 700;
    }

    .main p {
        font-size: clamp(1.25rem, 4vw, 2.5rem);
    }

    .main p:not(:last-of-type) {
        margin: 1rem 0;
    }

    .heart {
        display: inline-block;
        animation: heartbeat 1.2s infinite;
    }
</style>
