{
    "name": "vadymstebakov.github.io",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=16.0.0",
        "yarn": "please-use-npm",
        "npm": ">=7.10.0"
    },
    "scripts": {
        "dev": "vite --open --host",
        "build": "vite build",
        "preview": "vite preview --open --host"
    },
    "devDependencies": {
        "vite": "^3.0.7"
    }
}
